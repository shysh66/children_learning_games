# 🔄 מדריך מהיר לעדכון גרסאות

## 📌 3 מקומות לעדכן בכל גרסה חדשה:

### 1️⃣ src/App.jsx (שורות 4-5)
```javascript
// 📌 מספר גרסה - עדכן כאן!
const APP_VERSION = '1.1.0';    // ← שנה כאן
const LAST_UPDATE = '26.01.2025'; // ← שנה כאן
```

### 2️⃣ package.json (שורה 3)
```json
{
  "name": "children-math-game",
  "version": "1.1.0",  // ← שנה כאן
  ...
}
```

### 3️⃣ CHANGELOG.md (הוסף רשומה חדשה)
```markdown
## [1.2.0] - 28.01.2025

### ✨ נוסף (Added)
- תכונה חדשה מגניבה

### 🐛 תוקן (Fixed)
- תיקון באג X
```

---

## 🎯 מתי לעדכן מספר גרסה:

| סוג שינוי | דוגמה | גרסה לפני | גרסה אחרי |
|-----------|--------|-----------|-----------|
| 🐛 **תיקון באג קטן** | תיקון שגיאת חישוב | 1.1.0 | 1.1.1 |
| ✨ **תכונה חדשה קטנה** | הוספת נושא חדש | 1.1.0 | 1.2.0 |
| 🚀 **שינוי משמעותי** | שינוי מבנה המשחק | 1.1.0 | 2.0.0 |

---

## ⚡ תהליך עדכון מלא:

```bash
# 1. ערוך את הקבצים
# ...

# 2. עדכן את 3 המקומות למעלה
# - src/App.jsx (APP_VERSION, LAST_UPDATE)
# - package.json (version)
# - CHANGELOG.md (הוסף רשומה)

# 3. Commit & Deploy
git add .
git commit -m "Update to version 1.2.0 - [תיאור השינוי]"
git push
npm run deploy

# 4. המתן 1-2 דקות
# האתר יתעדכן אוטומטית!
```

---

## 💡 טיפים:

✅ **תאריך:** השתמש בפורמט DD.MM.YYYY (לדוגמה: 26.01.2025)
✅ **גרסה:** עקוב אחר [Semantic Versioning](https://semver.org)
✅ **CHANGELOG:** תאר מה השתנה בצורה ברורה
✅ **Commit message:** כלול מספר גרסה + תיאור קצר

---

🎮 בהצלחה!
